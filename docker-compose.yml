name: modasnancy

services:
  frontend:
    image: nginx:alpine
    container_name: modasnancy-frontend
    restart: always
    # NO expongas puertos — Nginx Proxy Manager hace el proxy interno
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
      - ./data/products:/products_data:ro
      - ./data/packages:/packages_data:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - nginx_proxy_manager_default
      - modasnancy-net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: modasnancy-backend
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    networks:
      - nginx_proxy_manager_default
      - modasnancy-net
    depends_on:
      - db

  db:
    image: mariadb:10.11
    container_name: modasnancy-db
    restart: always
    environment:
      - MARIADB_ROOT_PASSWORD=${DB_ROOT_PASSWORD}
      - MARIADB_DATABASE=${DB_NAME}
      - MARIADB_USER=${DB_USER}
      - MARIADB_PASSWORD=${DB_PASSWORD}
    volumes:
      - modasnancy_db_data:/var/lib/mysql
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - modasnancy-net

networks:
  nginx_proxy_manager_default:
    external: true
  modasnancy-net:
    driver: bridge

volumes:
  modasnancy_db_data:
